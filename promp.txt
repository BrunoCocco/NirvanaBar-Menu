Estamos continuando el proyecto freelance "NIRVANA BAR MENU".

Proyecto actual:
- Stack: Next.js App Router, React, TypeScript/TSX, Supabase, Tailwind, deploy en Vercel.
- El proyecto fue creado con `npx create-next-app@latest nirvana-bar-menu`, así que trabajamos con `.tsx` y `.ts`.
- Mantener esta estructura:

nirvana-bar-menu/
app/
  dashboard/page.tsx
  login/page.tsx
  globals.css
  layout.tsx
  page.tsx
components/
  MenuItemCard.tsx
  SpecialBanner.tsx
lib/
  supabaseClient.ts
database/
  schema.sql
.env.local
package.json
tsconfig.json

No cambiar innecesariamente estos nombres:
- tablas: public.menu_items, public.specials
- campos menu_items: id, name, description, price, category, available
- campos specials: id, type, title, description, price, start_time, end_time, updated_at
- values de specials.type: plato_dia, menu_semana, vino_casa

No cambiar innecesariamente estas variables y funciones:
- app/page.tsx:
  menuItems, specials, loading, errorMessage, loadMenuData, groupedMenu, platoDia, menuSemana, vinoCasa
- app/login/page.tsx:
  email, password, errorMessage, loading, router, handleLogin
- app/dashboard/page.tsx:
  specials, loading, errorMessage, router, checkSessionAndLoadData, handleChange, saveSpecial, handleLogout
- cliente Supabase exportado como: supabase

Estado actual:
- Supabase ya está creado y conectado.
- Las tablas y RLS/policies ya están hechas.
- El menú público ya funciona.
- El login ya funciona.
- El dashboard base ya está creado.
- La estética ya está definida: fondo oscuro, superficies crema/papel, acento mostaza/naranja, look editorial premium.
- Ya sabemos que faltan validaciones mejores en formularios, pulido visual del dashboard, revisión mobile QR y preparación para deploy.

Forma de trabajo:
- Quiero seguir construyendo a partir de este punto.
- Quiero que trabajes paso a paso.
- Quiero código comentado.
- Quiero explicaciones claras, pero sin rehacer lo ya decidido.
- No inventes arquitectura nueva.
- No cambies nombres salvo necesidad real.
- Si proponés mejoras, que sean incrementales sobre la base actual.

Tu tarea al retomar:
1. Hacé un resumen breve del estado actual.
2. Decime cuál es el siguiente paso técnico más lógico.
3. Continuá el desarrollo desde ahí.